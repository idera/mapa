1- Instalar drivers para sqlite (Ej: apt-get install php5-sqlite)
2- Instalar sendmails para envio de mails (Ej: apt-get install sendmail)
3- Debe existir el directorio /var/www/mapa/ogc (donde guarda el doc capabilities de cada servidor)
4- Ejecutar la primera vez el script monitor.php